# This file has moved to the project root: ../.env.example
# The actual .env is loaded from the project root by pydantic-settings (see core/config.py).
# Do not add variables here — edit the root .env.example instead.
